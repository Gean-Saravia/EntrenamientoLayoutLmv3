# ========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ========================================
# Copia este archivo como .env y completa con tus valores reales

# ============ SQL Server ============
DB_SERVER=(localdb)\MSSQLLocalDB
DB_NAME=facturas_db
DB_TRUSTED_CONNECTION=yes

# Para conexión con chat SQL
DB_CONNECTION_CHAT=Driver={ODBC Driver 17 for SQL Server};Server=(localdb)\MSSQLLocalDB;Database=facturas_db;Trusted_Connection=yes;

# ============ OpenAI ============
OPENAI_API_KEY=sk-proj-tu_clave_api_aqui

# ============ Generación de PDFs ============
PDFS_POR_FORMATO=2000
TEMPLATES_DIR=plantillas_facturas
LOGOS_DIR=logos_facturas
DATA_DIR=data

# ============ Evaluación de Modelo ============
MODEL_PATH=resultados/modelo_fold_3
IMAGES_FOLDER=test
OUTPUT_FOLDER=resultados_json

# ============ Entrenamiento ============
DATASETS_PATH=datasets_layoutlmv3
OUTPUT_PATH=resultados
NUM_FOLDS=5
NUM_LABELS=9
EPOCHS=1 (Si se quiere confirmar que el modelo funciona, 4 si queremos obtener mejores resultados)
BATCH_SIZE=4
LEARNING_RATE=5e-5

# ============ Preprocesamiento ============
ANNOTATIONS_PATH=annotations
